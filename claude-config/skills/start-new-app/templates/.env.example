# {Project Display Name} — environment variables
# Copy to .env (dev) or /opt/{project-name}/.env (prod, chmod 600)
# NEVER commit .env

# Application
APP_ENV=development          # development | production
LOG_LEVEL=INFO               # DEBUG | INFO | WARNING | ERROR

# Database
DATABASE_PATH=./data/app.db  # dev: relative; prod: /opt/{project-name}/data/app.db

# Secrets (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
SECRET_KEY=

# Brevo SMTP (if sending mail)
# BREVO_API_KEY=
# MAIL_FROM=no-reply@aqnas.xyz

# Cloudflare (for Caddy DNS challenge — prod only, usually in systemd EnvironmentFile)
# CLOUDFLARE_API_TOKEN=
