# This directory holds per-project infrastructure config:
#   - {project-name}.caddy   (generated by /start-new-app from caddy-config/template.caddy)
#   - {project-name}.service (generated by /start-new-app from systemd-service/template.service)
# Both are installed to /etc/ on the server by bootstrap-project.sh.
